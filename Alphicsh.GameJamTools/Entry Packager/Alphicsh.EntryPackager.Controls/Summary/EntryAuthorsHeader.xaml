<UserControl
  x:Class="Alphicsh.EntryPackager.Controls.Summary.EntryAuthorsHeader"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
  xmlns:local="clr-namespace:Alphicsh.EntryPackager.Controls.Summary"
  mc:Ignorable="d"
>
  <Grid HorizontalAlignment="Stretch">
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="Auto"/>
      <ColumnDefinition Width="2*" MinWidth="120"/>
      <ColumnDefinition Width="1*" MinWidth="120"/>
      <ColumnDefinition Width="2*" MinWidth="120"/>
    </Grid.ColumnDefinitions>

    <Button
      Grid.Column="0"
      Style="{StaticResource PrimaryButton}"
      Margin="2"
      Width="40"
      Command="{Binding AddAuthorCommand}"
    >+</Button>
    
    <Label Grid.Column="1" HorizontalAlignment="Center" Style="{StaticResource TableLabel}">Name</Label>
    <Label Grid.Column="2" HorizontalAlignment="Center" Style="{StaticResource TableLabel}">Community ID</Label>
    <Label Grid.Column="3" HorizontalAlignment="Center" Style="{StaticResource TableLabel}">Role</Label>
  </Grid>
</UserControl>
